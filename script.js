document.addEventListener('DOMContentLoaded', () => {
    const proposalText = document.getElementById('proposal');
    proposalText.style.animation = 'fadeIn 2s ease-in forwards, textGlow 1.5s ease-in-out infinite alternate';
});
